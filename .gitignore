# Build outputs
build/

# Keil MDK generated files (keep .uvprojx and .uvoptx, ignore binary outputs)
*.axf
*.bin
*.hex
*.elf
*.map
*.lst
*.htm
*.lnp
*.dep
*.crf
*.d
*.o
*.bak

# Keil uVision workspace (machine-specific GUI state)
*.uvguix.*

# STM32CubeIDE
.project
.cproject
.settings/
Debug/
Release/

# macOS
.DS_Store

# Windows
Thumbs.db
desktop.ini

# CMSIS headers — fetched at build time, not committed
# (kept local via the Makefile instructions in README)
